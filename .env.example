# No secrets required for static builds.
# After connecting GitHub + Sveltia OAuth, nothing else is needed in CI
# for a pure static Cloudflare Pages deploy.
#
# Optional local overrides:
# PUBLIC_SITE_URL=http://localhost:4321
