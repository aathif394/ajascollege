# Regenerated site map on every build via @astrojs/sitemap → /sitemap-index.xml
# When the custom domain goes live, update "Sitemap:" and astro.config.mjs `site`.
User-agent: *
Allow: /

# CMS admin UI — no need to index
Disallow: /admin/
Disallow: /admin

# Pagefind runtime assets
Disallow: /pagefind/

Sitemap: https://ajascollege.pages.dev/sitemap-index.xml
